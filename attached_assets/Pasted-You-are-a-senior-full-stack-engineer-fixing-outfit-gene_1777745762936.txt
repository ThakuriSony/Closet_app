You are a senior full-stack engineer fixing outfit generation logic in the Wearwise app.

CURRENT ISSUE:
The system generates invalid outfits (e.g., long dress + pants together).
This is unacceptable and must be prevented by logic, not AI guesswork.

GOAL:
Fix outfit generation so outfits are structurally correct, human-appropriate,
and follow real clothing rules.

IMPORTANT:
AI must NOT freely assemble outfits.
Outfits must be generated by deterministic rules.
AI may only explain or lightly rank results.

---

1. ADD OUTFIT STRUCTURE RULES (CRITICAL)

Define allowed outfit templates:
- DRESS_SHOES
- TOP_BOTTOM_SHOES
- TOP_BOTTOM_JACKET_SHOES

Rules:
- Dress CANNOT be combined with Top or Bottom
- Top + Bottom must always be paired
- At least Shoes must exist
- Jacket is optional and weather-based

Any outfit violating these rules is INVALID and must never be returned.

---

2. FIX FORMAL OUTFIT LOGIC

Formal outfits must be EITHER:
A) Dress-based:
- Dress
- Shoes
- Optional formal jacket

OR
B) Top/Bottom-based:
- Formal top
- Formal bottom (pants/skirt)
- Shoes
- Optional blazer

NEVER combine Dress + Bottom.
NEVER combine Dress + Top.

---

3. UPDATE generateOutfit()

Process:
Step 1: Decide outfit template first
Step 2: Filter items allowed by that template
Step 3: Apply occasion rules (Formal, Casual, Gym, etc.)
Step 4: Apply weather layering logic
Step 5: Score and select best items per category

The function must NEVER return an invalid structure.

---

4. UPDATE AI USAGE (IMPORTANT)

AI is ONLY allowed to:
- Explain why the outfit works
- Add human-friendly description

AI may NOT:
- Decide garment combinations
- Override structure rules

Example AI prompt:
"Explain why this outfit works for a Formal occasion in under 2 sentences."

---

5. ADD SAFETY CHECK

Before returning an outfit:
Run `isValidOutfit(outfit)`.

If invalid:
- Log error
- Retry with a different template

The app must NEVER show a broken outfit.

---

OUTPUT:
1. Updated outfitEngine.ts
2. Explicit validation logic
3. Safer AI usage
4. Testing steps (especially Formal cases)

CONSTRAINTS:
- Do NOT rely on AI for structure
- Do NOT overcomplicate
- Logic must be readable and deterministic

Start fixing now.